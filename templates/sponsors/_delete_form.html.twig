<form method="post" action="{{ path('app_sponsors_delete', {'id': sponsor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sponsor.id) }}">
    <button class="btn custom-button" style="margin-left: 5px;margin-top: 10px">
        <div class="custom-link-button">Delete Sponsor</div></button>

</form>
