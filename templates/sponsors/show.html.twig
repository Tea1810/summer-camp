{% extends 'base.html.twig' %}

{% block title %}Sponsors{% endblock %}

{% block body %}
    <h1>Sponsors</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sponsor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sponsor.name }}</td>
            </tr>
            <tr>
                <th>CommercialZone</th>
                <td>{{ sponsor.commercialZone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sponsors_index') }}">back to list</a>

    <a href="{{ path('app_sponsors_edit', {'id': sponsor.id}) }}">edit</a>

    {{ include('sponsors/_delete_form.html.twig') }}
{% endblock %}
